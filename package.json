{
  "name": "celld-demo",
  "private": true,
  "description": "place: a forward-only pixel board on celld — batched Durable Object writes, CDN-frame delivery",
  "license": "MIT",
  "devDependencies": {
    "esbuild": "^0.28.1"
  }
}
